@model LancheListViewModel

    <div><h2>@Model.CategoriaAtual</h2></div>

   <div class="container">
       <div class="row">
    @foreach(var lanche in Model.Lanches)
    {
        <div class="com-sm-4 col-md-4 col-lg-4">
         <p><img src="@lanche.ImagemUrl" class="img-fluid"/></p>
         <h3>@lanche.Preco.ToString("c")</h3>
         <h4>@lanche.Nome</h4>
         <p>@lanche.DescricaoCurta</p>
         
        </div>
    }
       </div>
    </div>

